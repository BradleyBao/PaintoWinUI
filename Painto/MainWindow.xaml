<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="Painto.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Painto"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:winuiex="using:WinUIEx"
    mc:Ignorable="d">

    <Window.SystemBackdrop>
        <winuiex:TransparentTintBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <!--<RowDefinition Height="50"/>-->
        </Grid.RowDefinitions>

        <!--<Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="20"/>
        </Grid.ColumnDefinitions>-->
        
        <!--<Canvas Grid.Row="0" Grid.Column="0" x:Name="DrawingCanvas" Background="Yellow" PointerPressed="Canvas_PointerPressed" PointerMoved="Canvas_PointerMoved" PointerReleased="Canvas_PointerReleased"/>-->
        <Canvas x:Name="DrawingCanvas" Background="Transparent" PointerPressed="DrawingCanvas_PointerPressed" PointerMoved="DrawingCanvas_PointerMoved" PointerReleased="DrawingCanvas_PointerReleased"/>
        <!--<Ellipse x:Name="Eraser" 
                 Width="30" 
                 Height="30" 
                 Fill="Transparent" 
                 Stroke="Black" 
                 StrokeThickness="1" 
                 Visibility="Collapsed"/>-->
        <StackPanel CornerRadius="5" Orientation="Horizontal" Grid.Row="1" Grid.Column="0" Height="50" Background="{ThemeResource CardStrokeColorDefaultSolid}" HorizontalAlignment="Center" VerticalAlignment="Bottom">
            <!--<Image Source="/Assets/sources/pen.svg" Height="100" />-->
            <GridView ScrollViewer.VerticalScrollBarVisibility="Hidden" Padding="0 3 0 0" Margin="2 0 0 0" ScrollViewer.VerticalScrollMode="Disabled" IsItemClickEnabled="True" ItemClick="RootTool_SelectItem">
                <GridView.Items>
                    <GridViewItem>
                        <FontIcon Glyph="&#xE8B0;" Tag="ComputerMode" />
                    </GridViewItem>
                    <GridViewItem>
                        <FontIcon Glyph="&#xE70F;" Foreground="#C72335" Tag="DrawMode" />
                    </GridViewItem>
                    <GridViewItem>
                        <FontIcon Glyph="&#xE75C;" Tag="Eraser" />
                    </GridViewItem>
                </GridView.Items>
                <GridView.ItemContainerStyle>
                    <Style TargetType="GridViewItem" BasedOn="{StaticResource DefaultGridViewItemStyle}">
                        <Setter Property="Margin" Value="0, 0, 0, 0"/>
                    </Style>
                </GridView.ItemContainerStyle>

            </GridView>
            
            <local:PenControl x:Name="penControl"/>

            <GridView ScrollViewer.VerticalScrollBarVisibility="Hidden" Padding="0 3 0 0" Margin="0 0 2 0" ScrollViewer.VerticalScrollMode="Disabled" SelectionMode="None" IsItemClickEnabled="True" Background="{ThemeResource ControlStrokeColorOnAccentDefault}">

                <FontIcon Glyph="&#xE710;" />
                <FontIcon Glyph="&#xED62;" />

                <GridView.ItemContainerStyle>
                    <Style TargetType="GridViewItem" BasedOn="{StaticResource DefaultGridViewItemStyle}">
                        <Setter Property="Margin" Value="0, 0, 0, 0"/>
                    </Style>
                </GridView.ItemContainerStyle>

            </GridView>
        </StackPanel>
    </Grid>
    

</Window>
